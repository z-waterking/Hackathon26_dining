# 2026-09-10 · 数据核验与提交推送

## 范围与发现

- 提交前检查当前工作区的反馈、聚合 Action、规则配置、已入库档口菜库、菜单与运行快照、API 数据投影和保存事务。只读检查正式库，回归写入仅使用隔离库。
- 找到并修复月度汇总仍只关联旧 `action.feedbackId` 的遗漏：新聚合事项使用 `feedbackIds`，造成批准/待审批计数漏计。修复按月份反馈集合求交集、每 Action 只计一次，排除演示项、兼容历史单反馈项，并补回归测试。不改已有反馈、审批或菜单。
- 新增可复用只读检查命令 `node tools/Verify-DiningData.mjs`，明确数据库 `readOnly/query_only`、一致性读取和拒绝写入的 service 投影。

## 正式数据与 API 核验

- SQLite 完整性通过，JSON/主键引用检查通过；2736 条菜品、17 个档口候选映射无悬空或跨档候选。
- 反馈共 209 条，其中 193 条真实单条反馈、12 条隔离演示、4 条汇总行；2 个转换批次共 278 条展示记录均可关联现有反馈，API 展示投影为正确十列结构；本次不重新解析原始 Excel 或逐单元格审核历史导出。
- 当前 1 项已批准且启用的排菜事项，前端资格判断、后端排菜、Prompt 管理投影一致；16 条启用规则进入最终 Prompt，未保存测试配置。
- 在线 `/api/data` 与菜品/反馈/Action 独立接口一致；反馈 dashboard 合计为 193，与排除演示及汇总行后的库记录一致；公开业务投影无内部 Prompt 字段。
- 历史提示保留原样：最早一个转换批次只生成 CSV，未记录 XLSX 路径，页面不提供不存在的 XLSX 下载；1 个历史单反馈 Action 不进入聚合；已保存菜单有 1080 个缺位；16 个历史运行包含 blocked/failed/cancelled。上述不等于数据损坏，也未被本次核验自动修复或改成成功。

## 回归与提交边界

- 完整单元/接口测试 349/349 通过，其中新增月度 Action 回归 4 项；完整桌面/手机浏览器测试 98/98、构建、lint、diff 格式检查通过。
- 确认没有进行中排菜后重启服务加载计数修复；只读核对 2026-07、08、09 三个月的批准/待审批统计全部匹配实时关联，服务健康正常。Git 操作不停止独立隧道。
- 不提交原始资料、Excel、运行数据库、环境密钥、转换产物、截图或临时运行文件。已对候选代码/文档匹配本地已配置秘密值，未发现匹配；运行中的服务与独立隧道不被 Git 操作修改。
- 保留现有分支 main，先同步远端状态，常规提交与推送，不 force push。
