# 2026-09-09 菜品串档与六周生成恢复

## 原因与修复

- 两次真实运行分别在第 3、4 周以“AI 菜单引用候选外菜品或错误档口”失败；失败前已保存 2、3 周。旧结构只限制全库索引上下界，不能让输出结构绑定档口。不是六周全部没有调用模型，也不是网络权限再次失败。
- `gpt-direct-v2` 使用固定 `S0…Sn` 对象，每档口完整独立候选目录、局部整数范围及精确输出长度；服务端映射回原始菜品 ID。上一周菜单、价格候选和固定菜品索引同步转换为局部格式。仍按整周生成，保留寻味列车/五味坊共同上下文，不抽样菜库、不引入随机替代结果。
- 使用 OpenAI Docs 技能核对 Structured Outputs 官方限制后，采用数值范围与定长数组，不使用超出总数上限的全库 enum。未更改 Azure 部署或模型版本。
- 每次模型响应在校验前保存为私有尝试记录；结构、菜品引用、人工菜单或固定出品等错误最多追加两次同周纠错，携带前次输出和安全定位。网络/超时/上游拒绝不自动重试。API 不暴露原始响应、Prompt 或密钥。
- 本次 `planner.usage` 包含纠错请求；`acceptedOutputUsage` 汇总最终六周成功批次；`reusedUsage` 单列复用历史成功周次，不重复算入本次调用。实际费用仍以 Azure 账单为准。

## 恢复与边界

- 菜单页“恢复生成记录”经统一 API 获取记录；失败保留当前菜单，继续时只在菜单页显示等待。
- 恢复创建新子运行，逐一验证连续成功周次；原失败记录完整保留。规则、固定出品、菜库、已批准 Action、配置及源文件变化时拒绝混用旧结果。v1 仅显式兼容已知原模板哈希；新的 v2 输出不接受旧结构。
- 六周已完成而检验失败时，仅重新检验；完整但未保存的运行可直接查看，不再次调用 GPT。页面恢复不自动保存或批准，业务规则不符合仍阻止通过。
- 恢复记录和结果读取统一通过 `query-service`，SQLite/Memory 契约测试确认相同公开投影、无 AI 调用、无业务写入，并在查询层过滤原始响应。
- 新工具 `tools/Resume-Real-Menu.mjs` 调用已运行的本地 HTTP 服务，不另开写库应用；先备份，检查旧成功周次内容完全一致、菜品零串档以及业务数据未改变；仅显式 `--save` 另存待审核草案。

## 验证

- 单元/集成：完整 223 项通过，覆盖局部索引、空候选、原始失败输出、纠错上限、网络不重试、断点恢复、旧模板迁移、快照变化、连续性校验、仅检验恢复、HTTP 互斥锁及统一查询层契约。
- 前端：恢复 API 客户端 6 项与直接菜单/恢复浏览器 14 项通过；全仓隔离浏览器 48/48 项通过（桌面、手机各 24 项，约 1.7 分钟）；桌面/手机压缩截图检查无溢出。未在浏览器测试中调用真实 AI。
- 构建与 lint 通过。代码未提交或推送，保留工作区其他已有修改。

## 真实 HTTP 恢复结果

- 重启现有 4317 服务并保持已有 LAN 配置；该服务进程的显式 Azure 连接验证返回 `gpt-5.6-sol`，91 input / 15 output tokens。未更改隧道或公网配置。
- 从 `MR-9505e1f1-625f-4b9c-a8db-e0da1d5b8edd` 恢复，生成新记录 `MR-e7205086-455d-46aa-83bb-0c3d8420604c`。原第 1–3 周内容完全一致；新第 4–6 周均首轮成功，无纠错调用，随后独立 GPT 检验完成。
- 共 4 次真实请求，322555 input / 19061 output tokens，不重新计费生成原成功三周。所有请求均来自已运行的 HTTP 后端，不是测试进程中的替代客户端。
- 17 档口、6 周、4380 个槽位，3840 个已选具体菜品，菜品档口错误为 0。工具验证反馈、Action、菜库、规则和配置未改变，原失败记录未覆盖。
- 已另存待审核草案 `P-de2986d9-fc27-4b1c-b1da-dab0ad75f20e`。GPT 检验建议 `revise`：仍有 184 条本地错误、1145 条提醒，包含人工上传档口未提供菜单、≥30 元无候选以及少量重复等问题；不代表已合规或可直接发布。540 个空槽位是源规则要求的人工菜单/缺价格候选，不使用假菜补齐。
- 联调前 SQLite 完整备份：`app/data/backups/dining-before-menu-resume-2026-09-09T07-23-41-378Z.sqlite`。没有删除任何业务记录。菜单页点击“草案记录”读取本次草案，或在“恢复生成记录”查看完整运行结果，无需再次调用 GPT。
- 最终分层改动后已在相同联网权限下重启服务；真实 HTTP health/list/result/saved-plan 均成功，4380 槽位可读取、`stale=false`、私有尝试字段不可见。规则问题明细为 180 条缺位聚合、2 条重复间隔、2 条同餐重复；保留检验阻断，不宣称已可发布。
