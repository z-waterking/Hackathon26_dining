# 2026-09-10 · 生成期间取消

## 修改

- Action 生成、六周菜单逐周生成及续排新增页内“取消生成 / Cancel generation”。请求后禁用重复点击，原生成请求确认取消才恢复操作；取消请求失败不假报成功，可重试。其他页面仍可访问。
- 统一 API 层新增按安全随机任务 ID 取消的接口，服务端加有界任务登记及提前取消 tombstone；重复 ID 不重复发起模型调用，晚到的旧取消不影响新任务。支持非 HTTPS 公司内网浏览器的安全随机 UUID。
- Azure 同步 Responses 调用使用组合取消/超时信号关闭上游连接。取消不自动重试；迟到结果不进入业务写入。模型、endpoint、密钥及 Prompt 未改，不调用真实 AI 测试。
- 菜单在逐周、逐档和检验边界检查信号，取消运行持久化为 cancelled，已有完整检查点保留。已有 Action/分析、保存菜单不覆盖。继续排菜创建新任务并验证旧快照；独立重新检验、定点修复和回复草稿不在本次新增按钮范围内。
- 已返回的模型用量照常保留；取消不代表已发生调用免费。刷新/关页不等同显式取消；登记表是单进程一小时保留，不是持久化调度器。

## 验证

- 使用 mock AI、内存仓库和独立浏览器 fixture 验证，未对真实业务数据执行生成/取消。
- 检查取消前起跑、请求中中止、迟到输出、已完成竞争、ID 重放/隔离、取消后恢复，以及页内交互与中英文。
- `npm test`：451/451 通过（取消、API、菜单恢复、背压和现有业务回归）。
- 浏览器 desktop/mobile 共 92 项最终通过：首轮 86 项通过，修复 3 处 fixture/旧断言后受影响 42 项重跑通过；新增取消交互 10 项均通过，含非 HTTPS 安全随机 ID fallback。
- `npm run lint`、`git diff --check`、`npm run build -- --logLevel warn` 通过。

## 本地启用

- 重启前两次只读确认 `menuRuns` 和 `aiUsage` 均无 running 任务，未取消用户既有任务。
- 一致性备份：`app/data/backups/before-generation-cancel-2026-09-10T03-41-28-673Z.sqlite`。重启后 11 张表的行数/内容 SHA256 与备份全部相同，SQLite integrity_check 为 ok。
- 更新服务已在 `http://127.0.0.1:4317` 及当前公司内网地址启动，health 与首页返回正常，前端资源 `index-BbzcJeYA.js`。未发送真实模型请求；未提交或推送。
