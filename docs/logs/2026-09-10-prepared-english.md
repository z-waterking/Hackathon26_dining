# 2026-09-10 · 英文预生成与本地展示

> 后续已按用户澄清由 `2026-09-10-source-language.md` 替代：只翻译开发界面，业务原文和词云不翻译；本记录仅保留修改过程，不再要求执行预翻译或授权外发。

## 请求

取消 `Translating…`，提前翻译、保存本地，用时直接展示。

## 修改

- 前端取消渲染时的翻译请求、队列、自动额度和重试；只读静态词表与 API 携带的本地英文缓存，缺失译文使用静态提示并保护未编辑字段。
- API client 在交付数据及菜单进度前加载译文；工作区与 Prompt 专用接口按准确来源合并，普通工作区不携带隐藏 Prompt。
- 服务端成功保存/生成后准备新增展示文本；准备失败不会让已完成的业务写入被重试，GET 与旧翻译查询均不调用模型。
- 新增 `tools/Prepare-EnglishTranslations.mjs`：默认只读检查；显式 `--apply` 备份数据库后准备译文，保留业务字段、审批、菜单、Prompt版本及原始工作表。
- 仅采集允许展示的说明字段，不把原始反馈正文/证据、菜单明细、菜名、档口或整个原始文件快照送入显示翻译。

## 验证

- 全量单元/API测试 397/397 通过；lint、build通过；浏览器中英文及排菜 Action 快照测试 22/22 通过，追加原始续帖保护后语言测试10/10通过（桌面+移动端，纯fixture）。
- 本地只读检查1084段待展示动态文本，其中67段已有缓存、1017段待准备；数值会随正在进行的菜单生成变化。没有假定这些内容已全部翻译。
- 原始线程续帖与转换表中混合的反馈原文逐行保留中文，操作说明继续读本地英文，不改转换记录或保存载荷。
- 实际批量翻译尚未执行：首次检查发现当前有用户菜单运行；随后出站自动安全审批拒绝了把内部 Action/规则/Prompt 文本发送到配置的 Azure endpoint 的并行准备请求。没有绕过限制，没有新增付费翻译调用。CLI保留空闲检查和仅允许写译文/AI用量的窄写入接口。
- 为保护正在生成的菜单，尚未重启4317后端，代码更新还未完成服务切换。需要确认允许发送上述说明文本后，在当前AI任务结束时批量准备本地缓存并重启验证；原始反馈正文/证据与菜单明细不作为翻译对象。
