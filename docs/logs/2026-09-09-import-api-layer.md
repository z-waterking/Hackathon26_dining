# 2026-09-09 原始文件导入入口与数据API规整

- 在反馈中心页首新增“导入原始文件并转换”按钮，打开说明弹层，选择原始Forms XLSX即可自动转换为十列目标表并入库；成功后展示新表、计数及下载。台账内旧上传入口保留并共用同一处理函数，不产生两套转换逻辑。
- 页面所有读取、写入、AI命令、Excel上传和下载地址改经`diningApi`统一业务客户端；独立HTTP transport负责base URL和错误处理，不在各页面散落路径和fetch。
- 服务端所有公开读取集中到Query Service，新增反馈/菜品/事项/菜单/批次的资源接口，保留`/api/data`启动快照兼容。内部Prompt过滤与本地访问限制未降低。
- 数据层拆为Repository契约与SQLite适配器；原store工厂继续可用，业务模块不再接触数据库驱动。增加独立Memory适配器证明查询/命令跨实现一致，不引入隐式故障回退。
- 数据库引入schema版本1，旧库就地标记，不改业务行；保持幂等导入、人工回复、审批和关联ID。现有全部变化仍为本轮工作区修改，未自动提交推送。
- 新增API客户端/数据端口/迁移契约测试，并将原件真实上传浏览器用例改为页首按钮进入；具体架构、接口与后续异步数据库/对象存储/CORS边界见`docs/API-DATA-LAYER.md`。

## 验证与运行

- 98项业务/API/数据契约/布局测试、18项桌面与手机浏览器测试全部通过；生产构建、lint及diff检查通过。浏览器用例实际上传13行原始XLSX，验证十列新表、分页、重传去重、人工回复保留及批次回看；新增转换服务失败后在弹层内显示错误并可重试的断言。
- 补齐共享Modal的可访问标题关联；Escape统一经过关闭回调，转换中不意外关闭弹层。导入主按钮增加绿色主操作与键盘焦点样式。桌面及390px手机截图已用1280px内压缩联图核对，原图保留在`app/test-results`。
- 定向停止旧4317服务后，使用SQLite一致性备份API保存`app/data/backups/dining-before-api-layer-20260909.sqlite`，再启动新版本。迁移前schema版本0，重启后版本1；11张表逐行序列化内容及SHA-256一致，`PRAGMA integrity_check`为ok。未重导、删除或修改业务记录。
- 现有209条反馈（197真实/12示例）、2733菜品、5项Action、人工回复与审批及9条历史AI用量均保持原样。本轮未调用真实Azure、未新增本地示例或真实反馈，测试使用隔离库。
- 本机首页与健康检查200，各资源GET和工作台快照一致，洞察统计193条真实单条反馈；转换批次新表为139行/10列，新批次XLSX与旧批次CSV下载均200。最早的历史批次原本无XLSX产物，继续只提供CSV，不伪造缺失文件。原始Prompt配置接口仍404。
- 新版本已运行于`http://127.0.0.1:4317/`；README补充API配置和架构文档入口。本轮未提交或推送。
