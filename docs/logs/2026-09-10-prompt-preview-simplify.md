# 2026-09-10 · 简化排菜 Prompt 预览

## 修改

- 按确认意见，移除排菜 System Prompt 区的“实际调用档口”选择器、切换状态和逐档执行说明，保留一个“最终 Prompt 预览”。
- 编辑框明确只维护排菜业务指令。预览展开后用四个简短标签说明内容来源：排菜指令、自动加入的规则、已批准 Action、输出格式与固定校验。桌面四列、手机两列。
- 全文直接显示 `previews.menuSystem`，不由前端解析或重新拼接；未保存修改不进入预览，成功保存使用服务端返回的新版本。
- 预览明确为全部可参与 Action 的总览；真实逐档请求仍只注入当前档口及全局适用事项。本次不改 API、规则、实际生成指令、模型配置或数据库。
- README 与 CHANGELOG 同步更新。保留工作区既有修改，未提交或推送。

## 验证

- `npm test`：345/345 通过；`npm run lint` 通过。
- `npm run build` 通过；定向桌面/手机浏览器回归 10/10 通过，验证单一预览、四个组成标签、使用全局服务端内容、保存前后更新及原有编辑/冲突处理；窄屏无页面水平溢出。
- `git -c core.safecrlf=false diff --check` 通过。
- 已运行服务只读检查：健康正常，首页与新 PromptSettings 脚本均为 200；脚本包含“最终 Prompt 预览”和全部四标签，不再包含档口选择器。仅更新前端构建，无需重启，不中断正在进行的业务。
- 本次未调用真实 AI、未向正式数据库写测试值，未改动已批准事项。
