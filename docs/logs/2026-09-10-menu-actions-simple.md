# 2026-09-10 · 简化排菜 Action 清单

## 修改

- 将六周菜单顶部的排菜Action面板改为紧凑列表，仅显示Action标题与具体排菜要求。删除四组统计、逐项状态、不可用原因、档口标签、服务事项和重复说明。
- 只显示既有选择器判定可参与排菜的正式事项；未批准、停用、缺少反馈关联或要求的事项不出现在清单。筛选逻辑、审批状态及排菜后端不变。
- 保留管理Action入口与一句空态；本地规则模式下只补一句不应用Action的提示。长列表限制高度并支持键盘滚动，不截断要求正文。
- 沿用中英文显示层，切换语言不改原值。未更改数据库、Prompt、历史菜单或生成流程，未调用真实AI。
- 同步README和数据层文档；保留上一轮英文改动与用户未提交文件。

## 验证

- 构建、lint、git diff --check通过；单元/API测试364项通过。
- 排菜Action、真实菜单工作流及中英文定向浏览器测试48项通过（桌面/手机）；覆盖审批和停用后的同步、有效项过滤、刷新保留、长文本滚动和显示译文不回写。所有测试API均mock。
- 桌面/手机中英文页面视觉检查通过，四张原始PNG保留在app/data/qa/2026-09-10-menu-actions-simple；检查用contact-sheet.jpg为1280×1220、122793字节。
- 本地4317服务健康，已提供最新构建，刷新即可查看。未提交推送。
