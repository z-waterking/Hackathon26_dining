# 2026-09-10 · 本地资料与Prompt版本功能提交

## 提交范围

- 汇总自上次提交以来已完成的界面中英文切换与业务原文保留、排菜Action简化、提交成功关闭弹窗、生成取消、菜品新增/编辑/归档恢复、资料导入及英文名本地持久化。
- 包含Prompt确认后联动最终预览、固定Base复原、完整历史版本追溯、统一API与事务/并发保护，以及对应测试和逐次修改日志。
- 不提交本地`.env`、数据库/备份、原始Excel、构建/测试产物；未跟踪参考图`image (1).png`及QA预览`docs/qa/menu-progress-preview.jpg`保留本地，不加入提交。

## 提交前核对

- 远端`origin/main`已fetch；准备提交时HEAD与远端一致，无需合并或改写历史。
- 549项自动测试通过，lint与生产构建通过；暂存差异格式检查通过。
- 独立只读边界检查：109个功能文本文件无本地真实密钥匹配，89个JS模块相对引用无缺失；再次检查Git暂存内容，不含数据库、密钥或生成图片。
- 只读业务数据校验`Verify-DiningData.mjs`通过，SQLite完整性正常。当前用户正在运行的菜单生成不停止、不重启服务；其业务记录和新写入数据不纳入Git。
- 浏览器全量回归180/180通过（桌面90、移动90），使用隔离测试库，不触碰生产业务数据、不调用真实模型。
