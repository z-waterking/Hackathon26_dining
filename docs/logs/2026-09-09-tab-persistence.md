# 2026-09-09 刷新保留当前主导航页

## 修改

- `app/src/Workbench.jsx` 原先每次加载固定`feedback`，现在从当前URL hash恢复页面，并初始化对应已访问页；继续保持已访问组件在页内切换时不卸载。
- `app/src/view-route.js`定义五个允许页面：feedback/actions/menus/catalog/analytics。切换主导航、首页横幅或页面跳转统一写入hash，刷新后直接回到该页。浏览器前进/后退及手改hash也同步选中状态。
- 使用当前浏览器标签页自身URL，不使用localStorage或跨标签页同步。只持久化导航，不保存业务原文、未提交表单、草稿、密钥或模型请求；刷新不会自动重新生成菜单或Action。
- 非法/空hash安全回退反馈中心，保留部署路径与查询参数，规范化使用replaceState，重复点击当前页不会堆积历史记录。
- 仅前端构建更新，不重启后端、不停止用户任务、不更改数据库或调用付费AI。

## 验证

- 新增3项单元测试覆盖五页白名单、畸形输入回退、路径/参数/历史状态保留、重复点击、独立标签页。全仓299项单元/集成通过，lint/build/diff检查通过。
- 全仓桌面/移动端Playwright72/72通过，包括新增10项导航用例：五页刷新保留、直接链接、前进后退、未知hash回退、同一浏览器多标签页互不覆盖。浏览器回归使用隔离服务和数据，断言不触发API写入或AI生成；旧用例无需修改。
- 实际4317服务首页、新前端资源及health均返回200；无需重启后端。未提交推送。
