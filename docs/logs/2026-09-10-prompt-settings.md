# 2026-09-10 · 可编辑 Prompt 与规则

## 实现

- 按最新请求，左下角新增“Prompt 与规则”，保留五个业务主导航，刷新维持 `#prompts`。四个编辑区与统一保存，不在业务卡片内混入Prompt编辑。
- 独立配置存入SQLite meta current/version-N及审计；严格schema、版本与基础来源指纹，旧窗口409冲突不覆盖新值；保留原表来源和运营规则覆盖。
- 真实Action聚合与真实逐档排菜消费已保存文本；System按范围逐条列出规则、批准Action与固定技术协议，预览复用实际builder并加入与Responses相同的边界文字。
- 新run快照固定规则/Prompt；运行中保存只作用下次，不重新提交已完成部分；新配置使旧菜单标记不再代表当前要求，恢复/复检不混用版本。Action缓存以实际Prompt哈希失效，已有人工决定不被重写。
- 人工/固定档口的控制属性来自不可变源表，不能因改写文字而悄悄取消；本地程序数量价位/重复/标签等校验独立只读列出。可编辑规则是AI业务规则，不声称任意文本会改写本地程序阈值。
- 专用API通过统一diningApi与查询层提供业务Prompt，其他接口继续脱敏；API密钥/环境变量不进入DTO。当前无管理员权限隔离，能访问应用者也可管理此配置，未额外扩大网络入口。
- 使用 OpenAI Docs 核对Responses instructions与本地版本化Prompt建议，保留配置的Azure endpoint/model，不创建托管Prompt对象、不调用模型进行本次测试。

## 验证

- `npm test`：345/345 单元与 HTTP 集成测试通过；配置服务 15 项、运行接入 10 项覆盖保存、版本冲突、来源追踪、快照隔离、缓存失效、真实调用构造和预览一致性。
- 浏览器回归：96 项完整桌面/手机 E2E 全部通过；另有 2 项实际隔离后端 DTO 的只读页面测试通过，共 98 项。覆盖四区编辑与统一保存、失败保留草稿、刷新定位、来源变化、规则分页及响应式布局。
- `npm run build`、`npm run lint` 与 `git -c core.safecrlf=false diff --check` 通过。
- 视觉检查：`app/data/qa/2026-09-10-prompt-settings/prompt-settings-contact-sheet.jpg`，1280×1000，118240 字节；桌面和手机无页面水平溢出。完整 PNG 原图保留在同目录。
- 保存测试仅使用隔离数据库与模拟模型；未调用付费 AI、未向正式库写测试配置，也未修改已有审批。
- 最终只读复核未发现新的阻断问题；确认没有进行中的菜单生成后重启本地服务，保留 Azure API 出网能力与原有内网入口。`http://127.0.0.1:4317/api/health` 返回正常，首页、主脚本与 PromptSettings 新脚本均为 200。
- 正式配置 GET 验证：版本 0（未保存测试值）、16 条来源规则、17 个档口预览、1 项当前符合条件的已批准 Action；`sourceChanged=false`，模型仍为 `gpt-5.6-sol`，无 API 密钥字段；普通数据接口继续只返回原业务 DTO。未调用真实生成。
- 未提交或推送，既有工作区改动和独立隧道保持不动。
