# 2026-09-09 真实AI改善事项

## 改动

- Action页移除示例加载、真实/示例切换、四张重复指标卡及大段流程说明；只保留真实AI生成、月份/审批状态筛选、事项清单和简短排菜入口。事项折叠时直接显示措施，展开编辑、查看证据与审批。
- 每次点击“AI 生成改善事项”显式请求`demo:false, force:true`。打开页面仅GET读取，不自动产生费用。请求中禁用重复操作；失败显示错误，保留已有事项，不回退Mock或伪造结果。
- 强化内部聚合指令：优先真实可改善问题，合并同类诉求，按“核验、措施、验收”给出具体步骤，周期/目标标为建议；不把历史投诉断言为当前事实，不虚构责任人、基线、改善效果，不强行凑满事项。不对运营暴露Prompt。
- 依据OpenAI Docs的Responses结构化输出要求，保留`text.format`严格JSON Schema、`store:false`及未完成/拒绝检测；Azure连接继续使用用户指定endpoint、`gpt-5.6-sol`和服务端密钥，未更换模型、未降低TLS验证。
  官方参考：[Structured model outputs](https://developers.openai.com/api/docs/guides/structured-outputs)。
- 首次真实联调成功收到模型响应，但模型改写了一个证据引用，严格校验阻止了入库。改为将全部原文切分为不超过400字符的连续段落，模型仅选择`evidenceIds`，服务端从同一原文快照回填quote。全文仍完整发送；未知ID、跨记录引用、重复引用继续拒绝，未放宽证据真实性约束。
- AI错误日志新增安全的HTTP状态、机器错误码及请求ID，不记录网关原始响应或密钥；区分权限、限流、部署地址、参数及输出上限，写请求不自动重试。

## 真实联调与数据

- 联调前一致性备份：`app/data/backups/dining-before-real-actions-20260909.sqlite`。
- 通过正式`POST /api/actions/summarize`分析193条真实单条反馈，实际Azure调用成功，生成并保存6个真实聚合事项，全部待审批：非辣菜与含椒标识、热餐温度巡检、上菜时长管理、餐具洗消抽检、食材接触规范、绿叶菜保供。每项有1–4段准确原文证据；未代替运营批准、执行或发布菜单。
- 最终请求ID：`resp_0e1a886b399fbb86016aa0d7a043e081978a562c69f90b3f1d`；部署`gpt-5.6-sol`，输入25947、输出3881 tokens。本轮两次真实模型调用合计输入49483、输出8083 tokens；第一次因证据校验拒绝业务入库。未配置单价，不估造美元费用。
- 既有5项历史Action逐行保留，新增6项真实聚合，未清库。209条反馈、2733菜品数量不变，既有导入、设置、菜单运行等未改。联调期间用户在页面更新了1条反馈的状态/负责人/跟进记录，该改动保留，没有从备份覆盖；最终分析基于更新后的反馈快照。
- 旧4个示例Action与12条示例反馈只作为历史隔离数据保留，Action页无示例入口、无示例内容，也不进入真实AI分析。底层模拟接口保留给隔离回归测试。

## 验证

- 101项单元/集成测试通过；18项桌面/手机E2E通过，新增错误重试/保留已审批事项的2项定向E2E复测通过。构建、lint、diff检查通过。
- 实际4317页面桌面和390px手机均展示6项真实事项，无示例文本、无JS错误、无横向溢出；原图保留`app/data/qa/real-actions-*.png`，按1280px内压缩预览视觉检查。
- 所有新事项证据逐字属于对应真实反馈，状态均pending，SQLite完整性检查ok；服务已重启。README与项目文档同步新入口与历史模拟边界。
