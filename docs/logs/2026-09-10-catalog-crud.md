# 2026-09-10 · 菜品资料增删改

## 修改内容

- 菜品资料页新增“新增菜品”，与编辑共用完整基础字段和标签表单；保存成功关闭，校验/服务错误保留草稿，防重复提交。新增后定位到新菜品，避免当前搜索或归档筛选隐藏刚建结果。
- 每行增加删除/恢复操作及确认弹窗；默认不显示已删除菜品，可筛选包含已删除或仅已删除。删除采用可恢复归档，不物理删除菜品、历史菜单引用、配方、源文件。
- 新增后端 catalog-service 通过 repository 写本地 SQLite，统一 API client 提供 CRUD；支持修订号冲突、同身份查重和事务审计，拒绝伪造ID/来源/归档元数据，菜单任务期间拒绝维护以保护当前生成。
- 人工新增/调整菜品明确进入对应档口候选池；停用/归档菜品不进入候选，原始 Excel 映射不修改、不让人工条目冒充文件来源。固定菜单/价位/标签核验要求保持。
- 改名后仍按 sourceName 读取原配方证据，首次身份变更保留 sourceIdentity。历史菜单记录与运行快照不改；现有菜单展示仍按当前菜品库取菜名/价格，库变更需要重新检验，不声称旧显示已做不可变版本化。
- 自研界面文案支持中英文，菜品、档口及来源等业务内容保持原文。未调用真实 AI、未向正式库添加测试菜品。

## 验证与启用

- `npm test`：482/482 通过，含新增CRUD、API到分档排菜候选的完整链路、历史引用/配方保留、并发版本、失败事务回滚、来源隔离和恢复。
- 浏览器四组回归（catalog-crud/workbench/modal-submit/language）48/48 通过；补充空核验依据的基础编辑后，catalog-crud/workbench 16/16 再次通过，最终新增覆盖2项，总覆盖50项桌面/移动交互。
- `npm run lint`、构建、`git diff --check` 通过。
- 重启前确认 `menuRuns`/`aiUsage` 均无运行任务；一致性备份 `app/data/backups/before-catalog-crud-2026-09-10T03-51-08-470Z.sqlite`。重启后11张表行数/内容SHA256全部未变，SQLite integrity_check=ok。
- 本地服务已在 `http://127.0.0.1:4317` 启用；health及首页200，最终资源 `index-BEs9ym3J.js`。未创建测试业务记录，未调用真实AI，未提交推送。
