# 2026-09-10 · 资料完整入库与核对

## 核对与实施

- 核对“菜品资料”的列表、配方、资料清单均由统一API读取SQLite，不是页面直接读Excel或样例数组。现库有2736个菜品售卖项、1416份配方、5394条原料明细、657条原表英文菜名。
- 使用Spreadsheets技能中的来源完整性与只读原则核对资料，不修改原Excel、不重新计算或掩盖既有公式错误。复用项目既有解析器，校验两个压缩包内文件一致性，读取17个Excel、72张工作表；新增源SHA256/字节数校验，避免解析缓存与文件不一致。
- 原2733个初始菜品ID和全部1416份配方已在现库，纯菜单库2614项映射无缺漏。既有两处固定出品来源修正不由旧资料覆盖。
- 新增显式增量导入服务与命令：默认预览，apply前备份；只补缺，不重置人工字段、反馈、Action、菜单及Prompt。按稳定身份和内容去重。
- 将完整工作表单元格、公式、合并信息及源文件指纹存入独立数据库快照，不再只有资料摘要。原始反馈文件仅留来源快照，不重导入为业务反馈。
- 修复“资料清单”弹窗硬编码16个工作簿/69张表的旧统计，改为按API返回的实际清单汇总；菜品资料页明示使用本地数据库。
- 快照不意味着所有原表行均为已核验排菜菜品；现有规则映射和人工标签条件继续生效。排菜规则文件的既有启动/生成同步行为未改。

## 执行与验证

- 正式执行已完成：19个文件指纹（17个Excel、2个压缩包）、72张完整工作表、22,564行、184,669个非空单元格入库；新增73条meta（72快照+1清单）及1条审计，54条既有清单仅统一路径分隔符。
- 现库菜品/配方已齐全，实际新增菜品0、配方0、译文0，不重复导入原有2733个初始菜品。现库合计2736项（含后续3项修复）、1416份配方、5394条原料、657个来源英文名。
- 备份：app/data/backups/dining-before-materials-import-2026-09-10T02-53-24-727Z.sqlite。备份及现库PRAGMA integrity_check均ok。
- 与备份逐条对比：feedback/dishes/plans/transactions/actions/settings/menuRuns/imports/aiUsage完全相同；除资料清单外，原meta未变，既有审计完整保留。未改人工标签、反馈回复、批准事项、规则/Prompt版本或菜单候选映射。
- 第二次相同apply返回unchanged，各新增/更新计数为0，不再备份或增加审计。只读Verify-DiningData通过；源表已有56个公式错误保留待核验，不伪造成有效值。
- 默认命令完整重新校验压缩包并解析后再次dry-run仍changed=false，验证快照哈希稳定及默认流程可用。
- 全量单元/API测试374项通过（新增10项导入保护测试）；构建、lint、git diff --check及中英文双端浏览器8项通过，验证资料统计从mock API动态求和、语言切换不更改数据。
- 正式API返回2736项菜品、17个工作簿、72张表、22,564行；服务健康，最新构建已在4317提供。运行服务无需重启，本次未触发真实AI调用。未提交推送。
