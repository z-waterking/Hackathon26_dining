# 修改日志

## 2026-09-10 · 提交前数据核验

- 修复月度汇总只按旧 `feedbackId` 关联、漏计新聚合 Action 的问题；按反馈关联列表与月份求交集，每事项计一次，排除演示数据并兼容旧记录。
- 新增 `tools/Verify-DiningData.mjs` 一致性只读校验，检查数据库完整性、引用、转换文件和 Prompt/Action 投影，不改业务数据。
- 核验范围、历史提示与测试结果见 `docs/logs/2026-09-10-data-audit-and-push.md`。

## 2026-09-10 · 简化排菜 Prompt 预览

- 移除“实际调用档口”下拉框及逐档说明，仅保留“最终 Prompt 预览”。
- 明示四部分组成：排菜指令、自动加入的规则、已批准 Action、输出格式与固定校验；全文直接展示服务端已保存的组合内容。
- 保留保存后下次生效、未保存草稿隔离与真实调用的 Action 档口筛选，不修改生成逻辑或配置数据。记录见 `docs/logs/2026-09-10-prompt-preview-simplify.md`。

## 2026-09-10 · 左下角 Prompt 与规则配置

- 新增独立配置Tab，四部分展示反馈→Action、全部AI排菜规则、排菜System、批准Action注入方式；可编辑统一保存，下一次生成使用新版本。
- System按档口逐条列规则，实际预览复用真实builder，包含该档口可用Action与只读输出协议；原始规则和运营覆盖分开存储，程序价位/来源等校验不被文本编辑绕过。
- 独立版本/来源并发锁与事务审计；进行中请求沿用捕获快照，旧菜单不自动重排，Action缓存按真实Prompt指纹刷新。配置接口只暴露业务文本，不开放密钥或原始模型响应。
- 实施和验证记录见 `docs/logs/2026-09-10-prompt-settings.md`。

## 2026-09-09 · 六周菜单常驻排菜 Action 面板

- 六周菜单页生成区域前固定展示排菜Action清单，不需要先生成菜单或打开弹窗；展示可用要求、待审批及未参与原因，非排菜改善单列统计。
- 与服务端复用同一参与判定，审批保存后共享数据刷新；入口跳转Action事项管理，不在菜单页自动批准或调用AI。
- 当前要求与生成时快照明确分开，旧菜单空记录不再笼统显示“没有已批准事项”。详细验证见 `docs/logs/2026-09-09-menu-actions-panel.md`。

## 2026-09-09 · AI 排菜默认入口、冲突分页与定点修复

- 菜单页默认启用真实AI，移除新“模拟排菜测试”入口，历史模拟读取保留。更新已入库说明，不再要求每次预处理原菜单。
- 评分详情合并本地冲突和AI发现，按档口、日期、餐次、冲突、核验、操作分栏，每页10条，支持档口/级别筛选。
- 新增统一API的真实AI定点修复：一次请求只改一个档口的一餐，全部六周重新校验，目标冲突必须减少且不能转移问题或引入未知标签。人工/来源/未定位问题不自动消除。
- 接纳修改后分数与检验失效、Action显示历史证据，需重新检验再保存；局部等待不锁住其他页面，失败保留原草案，成功替换写审计，不覆盖旧菜单。详情见 `docs/logs/2026-09-09-menu-conflict-repair.md`。

## 2026-09-09 · 刷新后保留当前导航页

- 五个主导航页改为URL hash路由，刷新/直接链接恢复当前页，前进后退同步页面和选中状态。每个浏览器标签页独立，不使用共享localStorage覆盖其他标签页。
- 无效地址安全回退反馈中心；保留原URL路径、查询参数及历史状态，重复点击不增加历史。原页内保留状态与后台生成行为不变，不自动保存草稿或重发AI请求。
- 前端构建已更新，无需重启后端或停止正在进行的业务。单元/集成299项、桌面/移动端72项、lint和构建通过；详细验证见 `docs/logs/2026-09-09-tab-persistence.md`。

## 2026-09-09 · 原菜单立即入库、运行时仅从数据库取菜

- 按用户澄清，将原菜单Excel现在一次性正式入库，不再启动/生成时反复预处理。2614个源身份中2613复用已有记录补完整来源，新增1条真实16/位菜品；保留原标签/停用/ID/顺序，库共2736条，33重复合并、2异常隔离。
- 新增只读预览/备份/事务导入命令和数据库候选投影。启动、生成、恢复、复检、菜单读取不依赖原菜单Excel文件；标签/active变化仍即时生效并校验快照，规则Excel独立同步不变。重复导入0新增/0来源更新，无覆盖人工数据。
- 用户确认后停止正在运行的菜单，保留完整第1周和18个已完成档口，标记取消及未知上游用量；导入后重启4317与LAN服务，没有自动重新生成或新增本次AI费用。
- 296项单元/集成、62项桌面/移动端测试通过，包含无源Excel运行、入库幂等/回滚、单位分离和DB快照安全。详情及备份位置见 `docs/logs/2026-09-09-stall-catalog-db.md`。

## 2026-09-09 · 按档口候选预处理、独立排菜与评分

- 预处理指定纯菜单库：12档来源映射、按档口/菜名/价格/单位去重、隔离异常、保留原单元格和SHA256。仅缓存元数据，保留停用和人工标签；5个未覆盖档口明确使用补充来源。
- 新流程按周逐档调用真实GPT，寻味先于五味，后一档仅从自己候选匹配共享菜单；保留逐周即时展示。可修复错误最多追加两次局部纠错，成功档口独立留痕与断点恢复，网络失败不自动重试。
- 六周独立检验完成后在结果前部显示客观分，目标80，权重30/25/20/15/10；不保底、不覆盖阻断。冲突与分项收进详情，批准Action显示版本、要求、实际菜名/周次/日期及未落实原因。
- 283项单元/集成、62项桌面/移动端E2E通过。真实部署gpt-5.6-sol分别生成寻味/五味第一周，两次均通过本档校验，未重跑整份六周、未保存业务菜单。详细用量、源表缺口及实施记录见 `docs/logs/2026-09-09-stall-menu-score.md`。

## 2026-09-09 · 逐周生成与即时展示

- 正式菜单从“六周全部完成才显示”改为每周生成、校验、保存进度后立即展示；上一批全部成功菜单输入下一周 AI，六周齐备再独立检验。
- 增加统一 API NDJSON 进度流与逐周 UI：显示已完成/正在生成/等待中的周次，只展示真实已完成部分；过程不阻塞其他页面。完整检验前预览只读，中途失败保留已展示周次与原草案，恢复先显示成功前缀。
- 保留旧 JSON 生成接口；付费请求不自动重发，浏览器断开后后台继续留痕，增加慢连接背压上限、未完成预览保存保护及安全字段过滤。详情见 `docs/logs/2026-09-09-menu-progressive.md`。
- 验证：243 项单元集成、54 项全仓桌面/手机浏览器测试、最终构建后20项菜单专项均通过。已重启本地服务；此次不重复调用付费 GPT，不更改真实业务记录。

## 2026-09-09 · 菜品串档与六周生成恢复修复

- 定位近期第 3/4 周失败：旧 schema 只约束全库整数范围，GPT 返回的合法索引可能属于其他档口。切换 `gpt-direct-v2`：固定档口 S 键、档口局部索引、精确槽位数量；完整菜库不抽样，寻味列车与五味坊仍在同一周请求中。
- 模型输出先留痕再校验；结构或引用错误最多追加两次同周纠错，保留成功周次，不静默补菜。网络/上游 API 失败不自动重试。按本次调用、历史复用和成功输出分别记录用量。
- 新增恢复记录列表、继续剩余周次、读取已完成结果接口与页面入口。恢复校验业务/源文件/Prompt 快照，显式兼容已知旧模板；不自动保存、不自动批准，页内等待不锁其他页面。
- 新增真实 HTTP 恢复联调工具，运行前备份、验证旧周次不变及零串档，按显式参数保存新草案。实现、验证及真实调用结果见 `docs/logs/2026-09-09-menu-resume.md`。
- 真实联调复用原成功 3 周，4 次 GPT 请求完成剩余 3 周及检验；17 档口六周共 4380 槽位、3840 个位置已选具体菜品，零串档，已保存待审核草案。184 条规则问题仍保留并阻止自动通过，未改反馈/Action/菜库/规则。完整单元集成 223 项、桌面/手机 E2E 48 项通过。

## 2026-09-09 · 六周生成首周连接失败修复

- 最新3次生成均在第1周约100ms内失败，底层`EACCES`、无Azure HTTP响应。根因是上次在受限工具环境中启动的驻留后端继承了出站限制，并非密钥、模型名称或六周输出长度错误。
- 已用获准Azure出站访问的权限重启同一4317服务，保持现有LAN配置；同一驻留进程真实调用gpt-5.6-sol返回成功。未重复生成六周菜单，未修改反馈/Action/菜品/已有草案。
- `EACCES/EPERM`现在显示可操作的后端权限提示；新增启动选项`--verify-ai`/`-VerifyAi`，默认不调用模型，显式启用才做小型诊断。回归测试覆盖502、单次请求、无替代菜单与业务数据保留。详见`docs/logs/2026-09-09-menu-network-permission.md`。

## 2026-09-09 · 源规则 System Prompt 与真实 GPT 菜单

- 正式流程从GPT给偏好、本地算法选菜，改为GPT逐周直接选定六周全部槽位，再独立调用GPT检验。完整有效菜库压缩成短引用，不抽样；失败不以本地或示例结果替代。
- 从本地规则Excel同步16条规则、4条固定主食及4条固定出品来源，保留单元格/餐次/SHA256。新增`app/server/prompts/menu-planner.md`，按源约束和已批准Action构造系统指令，运营仍不直接编辑Prompt。
- 修复宽窄巷子首次导入误读早餐示例的问题。新增只读预览/备份后仅增补工具，真实菜库已补入源表两种4元/100g麻辣烫；旧记录及人工字段保留。固定出品增加可信来源白名单和`FIXED_SOURCE`校验，保存/读取不能通过伪造来源绕过；价格候选白名单提供给GPT减少已知错选。
- 修正五味坊同餐共享、饺子50%午晚不同、南粉午晚与热菜近两天重复规则。人工菜单未上传、标签未知、规则/价位候选不足保持明确缺口。
- 默认真实GPT、页内等待、不静默降级；Action显示实际菜单位置而非虚构基线，固定主食附入显示和导出。实施与真实联调记录见`docs/logs/2026-09-09-gpt-menu.md`。

## 2026-09-09 · 改善事项卡片化

- 将折叠清单改成三列/两列/单列响应式卡片，以类型配色、图标、优先级、措施摘要、原文依据、关联反馈数及状态呈现真实事项；增加轻量悬停和键盘焦点反馈。
- 点击“查看与审批”进入完整详情，保留编辑、批准/拒绝、证据和历史。保存后同步刷新卡片与详情；生成动画仍限于Action页面，未改数据/API/Prompt或引入示例。
- 只读核实并向用户解释现有聚合Prompt；明确Action输入未包括完整菜库/规则，实际菜单需后续排菜与检验。详情见`docs/logs/2026-09-09-action-cards.md`。
- 验证：101项单元/集成、34项桌面/手机E2E通过，构建、lint与diff检查通过。覆盖筛选下批准保持详情、关闭不重开，压缩视觉预览已核对；4317刷新即可使用，未提交推送。

## 2026-09-09 · 等待动画仅作用于Action页面

- 按反馈移除全局模态、背景遮罩、滚动锁和焦点抢占，等待动画改为Action页面内的状态卡片。切换页面后动画随页面隐藏，请求继续，返回可查看进度或结果。
- AI生成不再占用全局busy，只锁定Action生成/编辑操作；反馈中心、菜品资料等页面继续可用。完成或失败均不强制跳转、不抢焦点。
- 并发刷新增加序号校验，防止其他页面较早的刷新覆盖新生成事项；等待条数保留点击时快照。测试使用隔离请求，不产生真实AI费用、不修改本地业务数据。
- 验证：101项单元/集成测试、24项桌面/手机E2E全部通过，构建、lint与diff检查通过；页面内动画截图经压缩核对，4317已提供新构建。

## 2026-09-09 · AI生成事项等待动画

- 点击“AI 生成改善事项”立即弹出居中等待层，显示双环旋转、AI图标、循环光条和反馈范围/条数，背景遮罩模糊；不显示虚构百分比或阶段进度。
- 等待层覆盖真实请求及结果刷新全过程，成功后显示事项、失败后显示错误并恢复操作；等待时阻止重复点击、背景操作和误关闭，保留既有事项。支持手机、键盘焦点恢复和减少动态效果设置。
- 本轮仅修改界面与测试，不调用真实AI、不改本地业务数据。详细记录见`docs/logs/2026-09-09-action-generation-wait.md`。
- 验证：101项单元/集成测试、22项桌面/手机E2E通过，构建与lint通过，压缩截图已核对；4317已提供新前端，刷新即可使用。

## 2026-09-09 · 真实AI改善事项与简化Action页

- 去掉Action页示例入口和重复看板，保留真实生成、月份/状态筛选、可编辑审批清单；卡片直接显示具体措施，失败保留既有事项。
- 使用用户指定Azure Responses服务和`gpt-5.6-sol`真实分析193条反馈，生成6个真实改善事项并入库，全部待审批，未自动执行。
- 修复真实输出中证据被改写的问题：模型选择完整原文段落ID，由服务端回填精确引用，继续严格校验。内部指令要求“核验、措施、验收”，不把历史诉求假定为今天仍发生。
- 联调2次真实模型调用；第一次引用校验未通过、未保存事项，第二次经正式API成功。历史数据和用户同期跟进修改均保留，已备份；示例只保留历史隔离记录。
- 101项单元/集成、18项桌面/手机E2E及2项定向复测通过；实际页面已检查6项真实内容，构建、lint、diff检查通过。详细调用、用量与数据核对见`docs/logs/2026-09-09-real-ai-actions.md`。

## 2026-09-09 · 按请求提交交付

- 合并交付此前完成的反馈中心四卡看板、真实词云、原始文件导入入口和统一API/数据层重构，保留相应模块日志及迁移说明。
- 提交前重新运行98项单元/集成测试全部通过；上一轮18项桌面/手机E2E、生产构建与lint均通过。
- 提交范围仅含代码、测试和文档；本地密钥、数据库/备份、原始Excel、测试产物和未跟踪参考图`image (1).png`不纳入版本。前述“未提交推送”为实现完成时状态，本次按用户后续要求提交至main并执行普通推送。

## 2026-09-09 · 原始文件导入按钮与统一数据API层

- 反馈中心页首新增“导入原始文件并转换”按钮与文件说明弹层，上传后直接转换为目标十列格式、入库并展示新表，可下载XLSX/CSV；台账旧上传入口共用逻辑。
- 所有页面的数据读写、上传及下载地址统一使用`diningApi`，HTTP transport集中配置base URL/错误/参数编码。服务端公开查询集中Query Service，新增各业务资源GET，旧`/api/data`保持兼容。
- 拆出Repository、SQLite适配器和独立Memory测试适配器，数据库schema版本规整不覆盖业务数据；保留审批、回复、证据及原ID。本地访问限制和内部Prompt隐藏保持不变。
- 说明迁移边界与配置见`docs/API-DATA-LAYER.md`，实施日志见`docs/logs/2026-09-09-import-api-layer.md`。
- 验证：98项单元/集成测试、18项桌面/手机E2E通过，构建、lint和diff检查通过；覆盖导入弹层、失败重试、转换后十列表、重传去重和回复保留，已检查压缩视觉预览。
- 数据：重启前一致性备份到`app/data/backups/dining-before-api-layer-20260909.sqlite`；schema从0升级至1，11张表的行内容/摘要完全一致，完整性检查ok。209条反馈、2733菜品及审批等均保留。4317新版服务与API读取/下载已验证；本轮无真实AI调用，未提交推送。

## 2026-09-09 · 精简反馈首页与四卡数据看板

- 按最后一项要求保留下方反馈台账，撤下“处理工作区 / 反馈台账”视图切换、三栏处理区、快捷入口、待处理/最近完成重复列表。进入页面直接显示反馈中心。旧表上传、新表展示、录入、筛选、导出、单条回复及跟进弹层继续可用。
- 首屏横幅改为AI生成Action入口：明确说明AI归纳全部反馈、生成可执行事项、由运营审批后生效，点击进入独立Action模块；首页不再展示“从反馈到改善事项”或提供生成/审批事项区域。
- 横幅下方前置四张规整仪表卡：类型趋势、类型分布、外置真实词云、处理概览，桌面2×2、手机单列。默认全部真实反馈，可按月份统一筛选；台账搜索和示例筛选不污染真实统计。
- 类型趋势改为“批评、建议、表扬、询问”等多序列；原“投诉”仅在图表与筛选展示归为批评，不修改原数据。全部范围按月、单月按周，月份精度的反馈不分配到虚构日期，未知类型与日期明确保留。
- 词云采用真实中文字形测量、权重字号和螺旋防碰撞布局，替换原横向词条排列；随容器变化重排，保留完整词语和悬停计数，月报弹层复用同一组件。
- 新增只读`GET /api/feedback/insights?month=`，提供全部/指定月份词频与类型/状态统计，排除示例、月汇总和隔离记录，不调用AI、不写缓存、不回传原文或内部配置。
- 删除两个已撤下的旧界面组件及旧词条云样式；源码可从Git历史恢复，业务记录与数据库未删除。详细实现见`docs/logs/2026-09-09-feedback-insights.md`和`2026-09-09-feedback-dashboard.md`。
- Action入口默认真实反馈范围，示例仍可在Action模块主动切换/加载。月报保留在统计工具栏，可在弹层选择月份；首页外置词云无需打开月报或调用AI即可查看。
- 验证：88项业务/API/布局测试、18项桌面/手机浏览器测试全部通过，生产构建、lint和diff检查通过。新增断言四卡数量、2×2/单列等高、Action跳转、无首页聚合操作、分类/月份口径、词云真实DOM边界无重叠及空态。修复Canvas字形高度与SVG字体边界差异导致的词云重叠。
- 视觉检查：桌面与390px手机截图经1280px内压缩后核对，原图保留在测试产物。4317服务已重启并验证新insights接口，全部月份显示193条真实单条反馈、40个热词（197条真实记录中4条月汇总不计入）；本轮未调用真实AI，未提交推送。

## 2026-09-08 · 聚合改善事项与独立 Action 模块

- 按用户纠正，将单反馈分析改为仅起草回复；Action 改为从全部真实反馈（可选月份）统一整理的少量共性事项，每次最多6项，可为0项，不要求逐条覆盖。保留关联反馈及原文证据，重算保留人工编辑与审批。
- 撤下“运营调试”及原始Prompt编辑器，新增独立“Action 事项”导航。运营只编辑事项标题、说明、档口、优先级、排菜调整要求和审批理由；修改已批准内容后重新待审。内部配置及模型输入不再由运营API返回，原settings接口停用。
- 根据参考图重构反馈总览：餐饮横幅、四色真实状态指标、快捷入口、待处理/最近完成、趋势及类型分布；新增反馈队列、处理回复、跨反馈热点三栏工作区。上传旧表后展示十列新表的入口与完整台账保持可用。
- 本地已加入12条明确标记的示例反馈，聚合为4个示例Action（增加轮换、减少频次、临时停排、服务流程）。全部保持待批准，未代替用户审批；原197条真实记录不变。重复加载保留修改，示例不进入真实词云/统计/正式排菜。
- 增加显式模拟排菜与再次检验，使用真实六周规则引擎及本地角色模拟，不调用Azure。内置批准示例真实影响菜品选择；修改示例要求后旧映射失效，明确报告未落实，不假装理解任意自然语言。
- 既有1条旧逐条待审批Action保留在SQLite作为历史，不删除、不自动批准；新模块和排菜只消费聚合事项，真实范围可重新汇总。每次Action编辑/审批继续留审计和版本记录。
- 验证：70项业务/转换/API测试、16项桌面/移动端E2E通过，生产构建与lint通过；覆盖真实seed→Approve→模拟六周→影响证据→再次检验，API用量保持不变。修复移动端测试中加载提示与保存提示的选择器歧义。
- 运行：4317旧版服务定向重启，新版健康检查通过；确认209条总反馈中197条真实、12条示例，4项示例全部待审批，`/api/settings`返回404。本轮未进行新的真实Azure调用。
- 最终视觉核对：桌面总览与三栏处理区、桌面/手机Action编辑页已检查压缩预览；修复总览测试截图被月份输入滚动至台账的问题。隔离记录在总览、工作区与汇总来源计数中统一排除；示例回复草稿不再标成AI生成。
- 详细记录见`docs/logs/2026-09-08-action-summary.md`、`2026-09-08-demo-aggregate-actions.md`、`2026-09-08-action-module.md`及反馈界面日志；README保留一键启动及操作说明。

## 2026-09-08 · 修正反馈入口为上传旧表

- 将反馈主入口改为用户上传任意文件名的旧格式 Forms `.xlsx`；系统接收文件后自动完成十列新格式转换、幂等入库和页面展示，不再要求用户把问卷放入仓库固定路径。
- 新增 `POST /api/feedback/upload` 和 `GET /api/imports/:id/rows`。上传原件按独立批次归档，转换后行关联反馈ID，十列表格中可继续跟进、查看最新回复，并下载新表。
- 停止服务启动时自动读取固定问卷；已有库、回复及审批保持可用。目标模板可用时沿用模板，无模板时使用内置十列格式。
- 上传限制10MB；限定xlsx格式、校验文件名和ZIP展开量，保留来源与隔离报告。无效上传不入库。
- 验证：48项业务/转换/API测试、12项桌面/移动端浏览器测试通过，构建与静态检查通过。新增真实上传浏览器用例覆盖任意文件名、十列映射、13行分页、筛选复位、重导幂等、回复保留、刷新后展示及历史批次入口。
- 视觉核对：检查桌面/手机新表压缩预览，完整十列在表格内部横向滚动，页面无横向溢出。

## 2026-09-08 · 反馈到排菜闭环

- 开始：核验 149 条问卷工作簿与运营记录模板；确认 Azure 模型部署名称为 `gpt-5.6-sol`，版本 `2026-07-09`。
- 数据：为反馈转换批次、回复、待审批行动、运营提示词版本、菜单检验记录与 AI 用量增加本地 SQLite 持久化。原数据库就地增加表，不清空已有记录。
- 接入：Azure Responses API 仅由服务端调用；密钥保存在忽略提交的本地环境文件。
- 并行修改的详情分别记录在 `docs/logs/2026-09-08-feedback-conversion.md`、`docs/logs/2026-09-08-menu-workflow.md`、`docs/logs/2026-09-08-ui.md`。
- 反馈实数对账：149条问卷 → 139条有效转换，10条既有试填隔离；88条与历史精确匹配，净新增51条。当前SQLite有197条反馈、2733个档口售卖项。再次转换139条全部跳过，已生成可下载的十列按月XLSX/CSV和来源JSON。
- 规则工作流：只有批准行动参与排菜；模型策略真实影响候选排序/禁用。保存菜单前从服务端恢复检验记录，拒绝客户端伪造结果；人工换菜、规则/标签/行动/Prompt变化使旧结果失效。
- 摘要质量：词云采用餐饮词表和真实菜名/档口长短语，保留否定表达，减少签名与口头词；按反馈条数而非重复词次数计数。
- 依赖：安装ExcelJS、JSZip兼容Forms的命名空间变体；UUID传递依赖升至修复版本，npm审计0已知漏洞。
- 验证：40项业务/转换/API/工作流测试、10项完整桌面/移动端浏览器测试全部通过，静态检查和生产构建通过。浏览器测试40.9秒自然退出，已修复Windows测试服务收尾卡住。
- Azure实际验证：部署`gpt-5.6-sol`连通性通过；合成反馈分析、Action提取、AI月报、排菜员及检验员四次调用成功（6530输入token、4971输出token），生成完整6周60槽位测试菜单。检验员返回需要修改，系统保留人工审核要求。测试使用内存数据库，不创建真实审批。
- 启动命令已实跑通过：`.\tools\Start-Dining.ps1`；本地4317健康检查通过，服务返回197条反馈和2733条菜库，AI已配置。
- 运行期用量与日志：`aiUsage`记录调用状态、token及可选单价估算；`audit`记录转换、回复、行动审批、分析及Prompt调整。150USD目前仅为预算参考，费率未确认不编造费用。
- 视觉核对：保留桌面/移动端关键界面原截图，使用`tools/Preview-QA.py`生成1280px内压缩联系表检查回复审批、词云、运营调试和检验报告；未改原始截图。
